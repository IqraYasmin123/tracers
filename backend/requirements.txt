# --- Web framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# --- Config ---
python-dotenv>=1.0.1

# --- Testing ---
pytest>=8.0.0
httpx>=0.27.0

# --- Used by the real pipeline service (not needed to run tests, only real inference) ---
Pillow>=10.3.0
matplotlib>=3.8.0

# For REAL inference (not needed to run `pytest`), also install the AI engine's dependencies:
#     pip install -r ../ai-engine/requirements.txt
# This installs torch, transformers, scikit-learn, etc. — everything TracerPipelineService
# needs the first time a route actually calls pipeline.analyze().
