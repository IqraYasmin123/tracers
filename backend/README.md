# TRACER Backend

FastAPI REST API — authentication, case management, and the bridge between the client apps and
the AI engine.

**Status:** not yet built — this is Module 9 in the roadmap (`docs/roadmap.md`). This folder
exists now so the repo structure is complete from Module 1, but contains no application code
yet. It will be filled in module-by-module: routing and inference endpoints (Module 9),
database models (Module 10), case/report endpoints (Modules 13–14), auth (Module 16).
