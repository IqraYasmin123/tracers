# TRACER Database

PostgreSQL schema, ER diagrams, and migrations for users, cases, evidence, AI results, reports,
and audit logs.

**Status:** not yet built — this is Module 10 in the roadmap (`docs/roadmap.md`). This folder
exists now so the repo structure is complete from Module 1, but contains no schema yet.
